# 1. Turn off old swap
swapoff -a

# 2. Remove old file
rm -f /swap.img

# 3. Create new 32GB file (32 * 1024 = 32768)
dd if=/dev/zero of=/swap.img bs=1M count=32768 status=progress

# 4. Secure it
chmod 600 /swap.img

# 5. Format and enable
mkswap /swap.img
swapon /swap.img

# 6. Verify
free -h
