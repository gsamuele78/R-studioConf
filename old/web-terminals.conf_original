# ----------------------------------------------------------------
# WEB TERMINAL SERVICES CONFIGURATION
# ----------------------------------------------------------------

# --- Web Terminal (ttyd) Configuration ---
# The port ttyd will listen on (must match Nginx config).
WEB_TERMINAL_PORT="2222"
# The system user to run the ttyd service as. 'www-data' is recommended for security.
WEB_TERMINAL_USER="www-data"
# The shell that ttyd will provide access to.
WEB_TERMINAL_SHELL="/bin/bash"
# Credentials for accessing the web terminal (user:password).
# Leave BLANK for no authentication (NOT RECOMMENDED for production).
WEB_TERMINAL_CREDENTIALS="admin:SecurePassword123"

# --- Web SSH (wetty) Configuration ---
# The port wetty will listen on (must match Nginx config).
WEB_SSH_PORT="2223"
# The system user to run the wetty service as.
WEB_SSH_USER="www-data"
# The host for wetty to bind to. '127.0.0.1' is essential for security when using a reverse proxy.
WEB_SSH_BIND_HOST="127.0.0.1"
# The default Linux user that wetty will attempt to SSH in as.
# This user must exist on the system.
WEB_SSH_CONNECT_USER="your_linux_user"