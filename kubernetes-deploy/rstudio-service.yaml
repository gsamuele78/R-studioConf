apiVersion: v1
kind: Service
metadata:
  name: rstudio
  namespace: botanical
spec:
  selector:
    app: rstudio
  ports:
    - protocol: TCP
      port: 8787
      targetPort: 8787
  type: ClusterIP
