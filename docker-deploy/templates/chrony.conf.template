# chrony.conf.template - Template for chrony configuration
# Placeholders: %%DRIFTFILE%%, %%MAKESTEP%%, %%RTC_SYNC%%, %%LOGDIR%%, %%FALLBACK_POOLS%%

# Drift file
# The location of the file containing the drift data.
driftfile %%DRIFTFILE%%

# Step the system clock if the adjustment is larger than 1 second
makestep %%MAKESTEP%%

# Enable RTC synchronization
rtcsync

# Log directory
logdir %%LOGDIR%%

# Fallback NTP pools
%%FALLBACK_POOLS%%
